import {InitialStateNewsType} from "./newsReducer";

let startState: InitialStateNewsType

beforeEach(() => {
    startState = {}
})

test('new message should be added to the end of messaagesData', () => {

})
